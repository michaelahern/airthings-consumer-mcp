{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "client",
            "preLaunchTask": "npm: build",
            "program": "${workspaceFolder}/lib/client.js",
            "skipFiles": [
                "<node_internals>/**"
            ]
        }
    ]
}