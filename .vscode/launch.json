{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "client",
            "preLaunchTask": "npm: build",
            "program": "${workspaceFolder}/bin/client.js",
            "skipFiles": [
                "<node_internals>/**"
            ]
        }
    ]
}